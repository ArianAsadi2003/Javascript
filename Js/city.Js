//! ----------Check City----------
function check() {
  let meassage;

  showMessage(meassage);
  input.focus();
}

//! ----------Show Meassge----------
function showMessage(meassage) {
  para.innerText = meassage;
}
